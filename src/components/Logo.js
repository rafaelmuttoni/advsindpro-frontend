import React from 'react'

const Logo = (props) => {
  return (
    <div
      style={{
        fontWeight: 700,
        color: '#fff',
        textDecoration: 'none',
        fontFamily: 'Roboto, Helvetica, sans-serif',
      }}
    >
      ADVSINDPRO
    </div>
  )
}

export default Logo
